# This is the final, correct configuration for Netlify.
[build]
  base    = "my_fiambond_react"
  # FIX: The publish directory is relative to the base directory.
  publish = "dist"
  command = "npm run build"
  
  [build.environment]
    NODE_VERSION = "20"
    VITE_API_URL = "fiambondapi-07kpwxjf.b4a.run"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200